
pipeline {
    agent any 
      stages {
        stage ("build now") {
            steps {
                sh """
                    ls -lrt
                    cd spring-boot-hello-world
                    mvn install
                    """
                
            }
        }
      }

}
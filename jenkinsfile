
pipeline {
    agent any 
      stages {
        stage ("build now") {
            steps {
                sh """
                    ls -lrt
                
                    mvn install
                    """
                
            }
        }
      }

}